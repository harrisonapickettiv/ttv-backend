type Cart {
  id: ID!
  user: User
  items: [CartItem!]!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CartItem {
  id: ID!
  quantity: Int!
  cart: Cart
  game: Game
  createdAt: DateTime!
  updatedAt: DateTime!
}
